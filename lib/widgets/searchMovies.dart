import 'package:flutter/material.dart';

class SearchMovies extends StatelessWidget {
  SearchMovies({required this.keyword});
  String keyword;
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
